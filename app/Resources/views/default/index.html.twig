{% extends 'base.html.twig' %}

{% block body %}
    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="{{ btnSubmit }}"
               class="btn btn-primary" />
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
