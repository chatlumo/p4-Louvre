{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Etape 1/4{% endblock %}

{% block body %}
    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="{{ 'app.step1.submit'|trans }}"
               class="btn btn-danger" />
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
