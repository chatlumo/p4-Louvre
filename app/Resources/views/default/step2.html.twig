{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ 'app.progress.step2'|trans }}{% endblock %}

{% block body %}
    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="{{ 'app.step2.submit'|trans }}"
               class="btn btn-danger" />
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
