{% extends 'base.html.twig' %}

{% block body %}
    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="{{ 'app.step2.submit'|trans }}"
               class="btn btn-primary" />
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
